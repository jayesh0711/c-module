#include<stdio.h>
 main()
{
	int i,j ;
	printf("enter two variables");
	scanf("%d%d", &i, &j);
        (i==20 ||j==20)?(printf("atlest one variable is having 20 \n")):(printf("both variables are not havimg 20 \n"));

	  (i==20 && j==20)?(printf("both are less tehn or equal to 40\n")):(printf("both are not less or equal to 40 \n"));
}

